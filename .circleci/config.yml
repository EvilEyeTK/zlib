version: 2
jobs:
  build:
    working_directory: /ws
    docker:
      - image: enumack/buildcontainers:gcc-8.1
    steps:
      - checkout
      - run: mkdir build && cd build && cmake .. && cmake --build .
workflows:
  version: 2
  build:
    jobs:
      - build
